import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

/**
 * TODO (Student Task):
 *
 * 1. Create your first page component
 * 2. Display a simple message on the screen
 * 3. Example: "System running"
 */
